# ffmpegImageToVideo

Generate an MP4 from a list of images (JSON array), optional audio, and parameters (duration, fps, width, height, bg). Uploads the rendered video to the blob store and returns the download path (also in `value`) plus metadata.
