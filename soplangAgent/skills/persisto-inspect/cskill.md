# persisto-inspect

Show a quick summary of Persisto storage: variables count, documents count, graph size, and the persistence folder.

## Input Format
No input is required.

## Output Format
Returns a human-friendly summary with key statistics.
