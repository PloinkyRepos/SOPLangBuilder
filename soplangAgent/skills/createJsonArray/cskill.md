# createJsonArray

## Description
Takes a list of arguments and returns a single string containing the JSON representation of an array of those arguments.

## Input Format
Provide `input` as either:

- an array of values
- a single scalar value, which will be wrapped into an array

## Output Format
Returns a JSON string representing the array.
