# inspect-mcp-usage

Read and display the contents of `/last-tool.log` (or a custom path).

## Input Format
Provide a string path. Example:

```
/last-tool.log
```

If the input is empty, `/last-tool.log` is used.

## Output Format
Returns the full text contents of the log file.
