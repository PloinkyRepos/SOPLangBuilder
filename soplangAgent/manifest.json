{
    "container": "node:20-bullseye",
    "profiles": {
        "default": {
            "install": "npm install",
            "postinstall": "apt-get update && apt-get install -y ffmpeg"
        }
    }
}
